package edu.greenriver.it.introduction;

/**
 * Class for Exercise - Introduction (IT333) Problem 4
 * @author Benjamin Arnold
 * @version 05-APR-2016
 */
public class ProblemFour {

	/**
	 * Main Method
	 * @param args
	 */
	public static void main(String[] args) {
		/*
		 * Sorry :(
		 * Didn't get to it.  Was a busy week but luckily
		 * 	the situation has resolved itself!  Start the
		 * 	next week off a bit better.
		 * 
		 * My idea was to sort the array and use a loop to
		 * 	check the mainarray[m] against temp[n] and if
		 * 	it's the same, ++ the value in temp[n], otherwise
		 * 	add it as temp[n+1].
		 */
	}
}
